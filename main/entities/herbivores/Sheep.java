package entities.herbivores;

public class Sheep extends Herbivore{
    protected Sheep(Double weight, Integer velocity, Integer limit, Double hunger) {
        super(weight, velocity, limit, hunger);
    }
}
