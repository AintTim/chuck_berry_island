package entities.herbivores;

public class Duck extends Herbivore{
    protected Duck(Double weight, Integer velocity, Integer limit, Double hunger) {
        super(weight, velocity, limit, hunger);
    }
}
