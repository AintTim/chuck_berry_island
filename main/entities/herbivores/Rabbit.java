package entities.herbivores;

public class Rabbit extends Herbivore{
    protected Rabbit(Double weight, Integer velocity, Integer limit, Double hunger) {
        super(weight, velocity, limit, hunger);
    }
}
