package entities.predators;

public class Wolf extends Predator{
    protected Wolf(Double weight, Integer velocity, Integer limit, Double hunger) {
        super(weight, velocity, limit, hunger);
    }
}
