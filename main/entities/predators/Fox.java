package entities.predators;

public class Fox extends Predator{
    protected Fox(Double weight, Integer velocity, Integer limit, Double hunger) {
        super(weight, velocity, limit, hunger);
    }
}
